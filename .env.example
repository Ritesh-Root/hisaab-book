# Optional: enables the LLM enrichment layer (finding narratives,
# unknown-format schema mapping). The app is fully functional without it.
# Server-side only — never expose this key to client bundles.
OPENAI_API_KEY=
